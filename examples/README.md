# Example Outputs

These files are small published examples of the parser output shape:

- [`docs-page.output.json`](./docs-page.output.json)
- [`blog-post.output.json`](./blog-post.output.json)

Regression fixtures and expected snapshots live in [`../fixtures`](../fixtures).
